/**
 * Claude implementation target.
 *
 * During IMPLEMENTATION_PLAN Phase 4:
 * 1. Install the current stable official MCP TypeScript server package.
 * 2. Create an stdio McpServer.
 * 3. Register the tools listed in CLAUDE.md.
 * 4. Use Zod schemas from src/types.ts or tool-specific schemas.
 * 5. Keep side effects behind provider adapters.
 * 6. Return compact JSON/text receipts, not image bytes.
 */

console.error('MCP server scaffold only. Ask Claude to complete IMPLEMENTATION_PLAN.md Phase 4.');
process.exitCode = 1;
